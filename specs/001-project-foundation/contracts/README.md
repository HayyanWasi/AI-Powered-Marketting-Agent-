# API Contracts: Project Foundation

This foundation feature does not define any API endpoints. API contracts will
be defined in subsequent features (campaign generation, company profile, etc.).

The foundation layer sets up the FastAPI application skeleton with health check
endpoint only. All route handlers are stubs awaiting feature implementation.